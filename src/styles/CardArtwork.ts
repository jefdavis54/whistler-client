import styled from 'styled-components'

const StyledCardArtwork = styled.div`
  display: flex;
  flex-direction: column;
  align-items: center;
  a, p {
  color: inherit;
  }
`

export default StyledCardArtwork